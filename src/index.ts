import SMA from './SMA';
import EMA, { quickEMA } from './EMA';
import RSI, { quickRSI } from './RSI';

const Indicators = {
    SMA, 
    EMA, 
    quickEMA, 
    RSI, 
    quickRSI
}; 

export default Indicators;